# --- Terraform directories ---
.terraform/
.terraform/*

# --- Terraform state files (never push, contains secrets!) ---
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# --- Crash logs ---
crash.log
crash.*.log

# --- Local override files ---
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- Sensitive variable files ---
*.tfvars
*.tfvars.json

# --- Plan files ---
*.tfplan

# --- Provider binaries ---
terraform-provider-*
*.exe
*.zip
*.msi

# --- IDE / OS files ---
.vscode/
.idea/
.DS_Store
Thumbs.db